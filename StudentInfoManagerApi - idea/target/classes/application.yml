spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/gradtrack_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
    username: root
    password: 123123

mybatis:
  type-aliases-package: com.rabbiter.si.entity
  mapper-locations: classpath:mapper/*.xml
  configuration:
    cache-enabled: false
    local-cache-scope: statement

server:
  port: 9451
